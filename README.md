# 开发日志

---

## 2026-02-23

- 尝试让 OpenClaw 把 Web Chat 功能从 Gateway 服务提取出来做成独立应用，但 OpenClaw + MiniMax 2.1 失败了，系统提示太复杂无法提取，尝试多次后仍未实现
- 尝试用 OpenClaw 控制浏览器搜索机票，发现只能识别 HTML DOM 对象，无法正确操作。比如能添加出发地但无法添加返回地址。最终可能需要借助图像识别功能才能实现可靠的浏览器控制
- 当前使用 MiniMax 2.1 模型（NVIDIA 提供），速度还可以。Kimi 2.5 上下文字数限制较短，有时会超限。MiniMax 2.1 用起来感觉可以
- 通过企业微信机器人聊天来自动更新 GitHub 内容，完全自动化，无需人工干预，这是 OpenClaw 能做到的事情
- OpenClaw 的优势是确实像一个人，但操作有时候比较傻，从"人感"角度来讲还不错，但功能上需要改进
- 测试了企业微信机器人模式、应用模式以及钉钉，OpenClaw 都无法发起定时任务和通知，无法实现最简单的日程安排通知
- OpenClaw 浏览器插件在各个平台控制效果都很差，只能打开标签页，无法控制内容，比如登录网页等操作无效
- 当前使用 Ubuntu 25，中文输入法虽笨但可用。安装了 Chrome 浏览器，用 OpenClaw 插件控制浏览器，但效果很差
- Ubuntu 25 是最适合作为 OpenClaw 服务端的版本，在虚拟机中作为客户端使用体验也很好
- Ubuntu 24.03 可以安装 OpenClaw，但界面和使用的中文支持不太好
- 在 Windows 下用 PowerShell 管理员模式运行 OpenClaw 官网命令，安装成功。认为 PowerShell 是可用的安装方式
- 在 Windows 下通过命令行安装 OpenClaw，也失败了
- 原本倾向于用国产操作系统（统信、优麒麟等），在虚拟机中尝试安装。但通过命令方式安装 OpenClaw 时遇到各种问题
- 重装系统后，由于是云端系统，想寻找控制浏览器的方法，最终决定在本机（实体机或虚拟机）安装 OpenClaw
- 最初主要想实现提醒功能，但无法实现。尝试了各种方法，由于 OpenClaw 本身更新速度较快，经过多次尝试后，服务器被搞死了，无法恢复响应。最终只能重新安装 OpenClaw。
- 最早想让小贾实现最简单的提醒功能，比如设闹钟。但尝试后发现无法实现，因为企业微信和小贾的连接是机器人布局方式，无法主动传递信息，只能一问一答，带来很大问题，无法自动发信息。
- 最早使用腾讯云直接创建 OpenClaw 服务器，创建过程比较顺利。在轻量服务器上自主创建，支持企业微信等多种对话 channel，整个过程比较方便。
- 之所以想做这个项目，是因为安装了 OpenClaw 后，感觉它确实很像一个人，所以可以尝试一下。
- 全自动 AI 助手：通过全自动方式创造小贾应用

## 2026-02-20

- 现在我们开始
